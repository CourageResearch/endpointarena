# Repo-local Railway CLI auth.
# Prefer a project token so local commands stay scoped to endpoint-arena production.
RAILWAY_TOKEN=

# Use this instead only if you need account/workspace-scoped Railway commands.
# RAILWAY_API_TOKEN=
